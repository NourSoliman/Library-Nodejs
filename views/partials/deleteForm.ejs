<% if(req.user && author.user&& author.user.equals(req.user.id)|| user.role === 'admin') { %>
<form method="POST" action="<%= url %>?_method=DELETE">
    <button type="submit"  class="delete-button">
    <button type="submit" >
        delete
    </button>
</form>
    <% } %>