<h2>Recently Added</h2>
<div>
    <% books.map((i) => { %>
        <div>
            <a href="/books/<%= i.id %>"> 
            <img src="<%= i.coverImagePath %>" height="100px" width="150px">
        </a>
        </div>
        <% }) %>
</div>